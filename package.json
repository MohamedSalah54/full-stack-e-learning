{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "build": "concurrently \"npm run build --prefix frontend\" \"npm run build --prefix backend\"",
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run start:prod --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
